<Application x:Class="Argus_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Подключаем базовые стили MahApps.Metro -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Кастомные цвета и стили -->
            <SolidColorBrush x:Key="PageBackgroundBrush" Color="#FAFAFA"/>
            <SolidColorBrush x:Key="CardBackgroundBrush" Color="White"/>
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="#2d3436"/>
            <DropShadowEffect x:Key="CardShadow" BlurRadius="10" ShadowDepth="2" Color="#33000000"/>

            <!-- Стиль кнопки -->
            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="#0984e3"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="10 6"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Стиль чекбокса -->
            <Style TargetType="CheckBox" x:Key="SettingsCheckBox">
                <Setter Property="Foreground" Value="#2d3436"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="0 5"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
